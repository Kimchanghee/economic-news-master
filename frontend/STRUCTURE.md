# 프론트엔드 프로젝트 구조

이 문서는 프론트엔드 프로젝트의 파일 구조와 각 파일의 역할을 설명합니다.

## 📁 디렉토리 구조

```
frontend/
├── app/
│   ├── components/          # 재사용 가능한 UI 컴포넌트
│   │   ├── ui/             # 메인 페이지 UI 컴포넌트
│   │   │   ├── Header.tsx              # 헤더 컴포넌트 (로고, 언어 선택, 로그인 버튼)
│   │   │   ├── LanguagePicker.tsx      # 언어 선택 드롭다운
│   │   │   ├── CategoryFilter.tsx      # 카테고리 필터 버튼 그룹
│   │   │   ├── NewsCard.tsx            # 뉴스 카드 컴포넌트
│   │   │   ├── NewsGrid.tsx            # 뉴스 카드 그리드 레이아웃
│   │   │   ├── AdminBadge.tsx          # 관리자 페이지 안내 배지
│   │   │   ├── Footer.tsx              # 푸터 컴포넌트
│   │   │   ├── LoadingSpinner.tsx      # 로딩 스피너
│   │   │   ├── EmptyNewsMessage.tsx    # 뉴스 없음 메시지
│   │   │   └── PageSection.tsx         # 페이지 섹션 래퍼
│   │   │
│   │   └── admin/          # 관리자 페이지 전용 컴포넌트
│   │       ├── LoginForm.tsx           # 관리자 로그인 폼
│   │       ├── Dashboard.tsx           # 관리자 대시보드 메인
│   │       ├── DashboardHeader.tsx     # 대시보드 헤더
│   │       ├── TabButton.tsx           # 탭 전환 버튼
│   │       ├── StatCard.tsx            # 통계 카드
│   │       ├── CategoryDistribution.tsx # 카테고리 분포 차트
│   │       ├── CollectButton.tsx       # 뉴스 수집 버튼
│   │       ├── CollectionLogsTable.tsx # 수집 로그 테이블
│   │       ├── DashboardTab.tsx        # 대시보드 탭 콘텐츠
│   │       ├── NewsTab.tsx             # 뉴스 관리 탭 콘텐츠
│   │       └── NewsManagementTable.tsx # 뉴스 관리 테이블
│   │
│   ├── constants/          # 상수 정의
│   │   ├── categories.ts   # 뉴스 카테고리 정의
│   │   └── languages.ts    # 지원 언어 정의
│   │
│   ├── hooks/              # 커스텀 React 훅
│   │   ├── useNewsData.ts      # 뉴스 데이터 fetch 훅
│   │   ├── useNewsActions.ts   # 뉴스 좋아요/조회 액션 훅
│   │   ├── useAdminAuth.ts     # 관리자 인증 훅
│   │   └── useAdminData.ts     # 관리자 데이터 fetch 훅
│   │
│   ├── types/              # TypeScript 타입 정의
│   │   ├── news.ts         # 뉴스 관련 타입
│   │   └── admin.ts        # 관리자 관련 타입
│   │
│   ├── utils/              # 유틸리티 함수
│   │   ├── api.ts          # API 호출 관련 유틸
│   │   └── dateFormatter.ts # 날짜 포맷팅 함수
│   │
│   ├── [lang]/             # 다국어 랜딩 페이지
│   │   ├── locales/        # 다국어 컨텐츠
│   │   └── page.tsx        # 다국어 랜딩 페이지
│   │
│   ├── admin/              # 관리자 페이지
│   │   └── page.tsx        # 관리자 페이지 메인
│   │
│   ├── news/               # 뉴스 상세 페이지
│   │   └── [id]/
│   │       └── page.tsx    # 뉴스 상세 페이지
│   │
│   ├── page.tsx            # 메인 홈페이지
│   ├── layout.tsx          # 루트 레이아웃
│   ├── globals.css         # 전역 스타일
│   ├── manifest.ts         # PWA 매니페스트
│   ├── robots.ts           # SEO robots.txt
│   └── sitemap.ts          # SEO 사이트맵
│
├── public/                 # 정적 파일
├── Dockerfile              # Docker 이미지 빌드 설정
├── next.config.js          # Next.js 설정
├── package.json            # npm 패키지 정의
├── postcss.config.js       # PostCSS 설정
├── tailwind.config.ts      # Tailwind CSS 설정
├── tsconfig.json           # TypeScript 설정
└── STRUCTURE.md            # 이 파일
```

## 🎯 주요 기능별 파일 위치

### 메인 페이지 (홈페이지)
- **진입점**: `app/page.tsx`
- **컴포넌트**: `app/components/ui/*`
- **훅**: `app/hooks/useNewsData.ts`, `app/hooks/useNewsActions.ts`
- **타입**: `app/types/news.ts`

### 관리자 페이지
- **진입점**: `app/admin/page.tsx`
- **컴포넌트**: `app/components/admin/*`
- **훅**: `app/hooks/useAdminAuth.ts`, `app/hooks/useAdminData.ts`
- **타입**: `app/types/admin.ts`

### 뉴스 상세 페이지
- **진입점**: `app/news/[id]/page.tsx`
- **타입**: `app/types/news.ts`

### 다국어 랜딩 페이지
- **진입점**: `app/[lang]/page.tsx`
- **다국어 컨텐츠**: `app/[lang]/locales/*`

## 🔧 설정 파일

- **TypeScript**: `tsconfig.json` - 경로 별칭 `@/*` 설정
- **Tailwind CSS**: `tailwind.config.ts` - 디자인 시스템 설정
- **Next.js**: `next.config.js` - standalone 출력, 환경 변수 등
- **Docker**: `Dockerfile` - 프로덕션 빌드 설정

## 📝 네이밍 규칙

- **컴포넌트**: PascalCase (예: `NewsCard.tsx`, `DashboardHeader.tsx`)
- **훅**: camelCase with `use` prefix (예: `useNewsData.ts`)
- **유틸리티**: camelCase (예: `api.ts`, `dateFormatter.ts`)
- **상수**: camelCase (예: `categories.ts`, `languages.ts`)
- **타입**: camelCase (예: `news.ts`, `admin.ts`)

## 🚀 개발 가이드

### 새 UI 컴포넌트 추가
1. 메인 페이지용: `app/components/ui/` 에 추가
2. 관리자 페이지용: `app/components/admin/` 에 추가

### 새 커스텀 훅 추가
1. `app/hooks/` 디렉토리에 `use` 접두사로 시작하는 파일 생성
2. 관련 비즈니스 로직을 훅으로 분리

### 새 타입 추가
1. `app/types/` 디렉토리에 도메인별로 타입 파일 생성
2. export로 타입을 내보내기

### API 호출 추가
1. `app/utils/api.ts` 에 공통 API 함수 추가
2. 또는 새로운 훅에서 API 호출 로직 구현

## 💡 베스트 프랙티스

1. **컴포넌트는 단일 책임 원칙** 준수
2. **재사용 가능한 로직은 커스텀 훅**으로 분리
3. **타입은 명확하게 정의**하고 공유
4. **상수는 별도 파일**로 관리
5. **경로 별칭 `@/`** 사용으로 import 경로 단순화

